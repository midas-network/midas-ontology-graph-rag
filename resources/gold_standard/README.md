# Gold Standard Resources

Canonical gold-standard evaluation datasets live under:

- `resources/gold_standard/datasets/current.json`

Legacy dataset path `resources/test_abstracts.json` is kept for compatibility, but new edits should go to `datasets/current.json`.
